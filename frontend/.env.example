# Copy to .env and restart: npm run dev
# IMPORTANT: this file must live in frontend/.env (NOT backend/.env)

# Deployed backend URL (no trailing slash)
VITE_API_URL=https://featherflow-x17a.onrender.com

# false = use backend API | true = offline localStorage only
VITE_USE_LOCAL_STORAGE=false
